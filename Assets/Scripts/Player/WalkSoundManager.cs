using UnityEngine;

// DO LATER
public class WalkSoundManager : MonoBehaviour {
    /*public AudioClip[] grassSteps, rockSteps, waterSteps;

    Transform rootPlayer;
    AudioSource source;

    public void Start() {
        rootPlayer = transform.parent.parent;
        source = rootPlayer.GetComponent<AudioSource>();
    }

    public void Update() {
        bool moving = rootPlayer.GetComponent<Rigidbody>().velocity.sqrMagnitude > 0;
        if (!source.isPlaying && moving) {
            
        }
    }

    private Transform getUnderPlayer() {
        RaycastHit hit;
        Physics.Raycast(rootPlayer.position)
    }*/
}
